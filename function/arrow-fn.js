// function キーワードによる関数式
const plusOne = function (n) {
    return n + 1;
}

// アロー関数式
const addOne = (n) => {
    return n + 1;
};

// アロー関数式、さらに省略記法（引数がひとつだけの場合はカッコが省略可、本文が　return文だけの時はreturn文をブロックごと省略）
const increment = n => n + 1;

